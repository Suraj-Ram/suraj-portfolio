import React from 'react'

function ErrorPage() {
  return (
    <div class="grid h-screen place-items-center">
        <div>
        <span>The requested page doesn't not exist</span>
        </div>
    </div>
  )
}

export default ErrorPage